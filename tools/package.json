{
  "_comment": "Resume Craft for Claude Code — Node.js 格式转换工具。docx: JSON→DOCX生成 | puppeteer: HTML→PDF渲染",
  "author": "MistyBridge",
  "name": "resume-skill-tools",
  "version": "1.0.0",
  "description": "Node.js tools for resume generation — DOCX and PDF export",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "gen-docx": "node gen-docx.js",
    "gen-pdf": "node gen-pdf.js",
    "postinstall": "npx puppeteer browsers install chrome",
    "setup": "npm install"
  },
  "dependencies": {
    "docx": "^9.2.0",
    "puppeteer": "^24.4.0"
  }
}
